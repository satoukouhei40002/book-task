
<header>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <div class="navbar-bran">読書習慣化アプリ</div>
      <div class="header-btn-right">
        <% if user_signed_in? %>
          <div class="header-btn-right-left">
            <%= link_to '/reads/new', class: 'new' do %>
              <button type="button" class="btn btn-primary">読書を始める</button>
            <% end %>
          </div>
          <%= link_to 'ログアウト', "/users/sign_out", data: { turbo_method: :delete },  class: "btn btn-primary logout" %> 
        <% end %>
      </div>
      </div>
    </div>
  </nav>
</header>